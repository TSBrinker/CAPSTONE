import React, { useState, useEffect } from "react";
import axios from "axios";
import useAuth from "../../../hooks/useAuth";

const HouseholdCategoryContainer = ({}) => {
  return <div>Just like the other one but householdy</div>;
};

export default HouseholdCategoryContainer;
